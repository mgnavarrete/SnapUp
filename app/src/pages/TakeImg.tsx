import React from 'react';
import { Container, Typography } from '@mui/material';

const TakeImg: React.FC = () => {
  return (
    <Container>
      <Typography variant="h1">Tomar Foto</Typography>
      <Typography variant="h6">Opción temporalmente deshabilitada</Typography>
      
    </Container>
  );
};

export default TakeImg;
